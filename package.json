{
  "name": "njau-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lightweight allowlisted HTTPS egress proxy for NJAU campus network access.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check examples/cloudflare-worker/src/index.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@fastify/rate-limit": "10.3.0",
    "dotenv": "17.4.2",
    "fastify": "5.8.5",
    "ipaddr.js": "2.2.0",
    "undici": "8.3.0"
  }
}
